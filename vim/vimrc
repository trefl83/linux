execute pathogen#infect()
syntax on
filetype plugin indent on
set number
if has('gui_running')
	set background=dark
	colorscheme solarized
	set lines=35
	set columns=110
else
	colorscheme zellner
endif
